# macOS Finder metadata
.DS_Store

# Rust build output (solver-rs)
/solver-rs/target/

# Regenerable dev-tool artifacts — recreate with `node export-levels.js` /
# `generate` (solver-rs), never hand-edited, not the source of truth (levels.js is).
/levels.json
/solver-rs/generated-level.json
